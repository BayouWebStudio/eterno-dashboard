{
  "buildCommand": "cd client && npm install -g pnpm && pnpm install && pnpm exec vite build --outDir ../vercel-output",
  "outputDirectory": "vercel-output",
  "installCommand": "echo skip",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
