{
  "buildCommand": "cd client && npm install -g pnpm && pnpm install && pnpm exec vite build",
  "outputDirectory": "client/dist",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}